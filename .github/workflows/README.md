# GitHub Workflows
## Purpose
Contains CI/CD workflows, automated testing scripts, lint checking, and building configurations.
## Allowed Files
- YAML workflow configurations (.yml)
## Naming Conventions
- kebab-case (e.g. `ci.yml`, `deploy.yml`)