# src/styles
## Purpose
Contains CSS configurations, global variables, custom resets, and stylesheet layers.
## Allowed Files
- CSS files (e.g. `global.css`, `variables.css`)
## Naming Conventions
- kebab-case (e.g. `global.css`)