# src/config
## Purpose
System level static configs and integration settings.
## Allowed Files
- Configuration files (e.g., `site.ts`)
## Naming Conventions
- camelCase or kebab-case (e.g. `site.ts`, `auth-config.ts`)