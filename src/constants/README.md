# src/constants
## Purpose
System-wide immutable values, lookup dictionaries, and static constants.
## Allowed Files
- Constants (e.g. `routes.ts`, `colors.ts`, `roles.ts`)
## Naming Conventions
- kebab-case (e.g. `routes.ts`)
- UPPERCASE for constant variables inside files