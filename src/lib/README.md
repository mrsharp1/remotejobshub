# src/lib
## Purpose
Third-party client initializations and library wrappers (e.g. Supabase, query clients).
## Allowed Files
- Service libraries (e.g. `supabase.ts`, `query-client.ts`)
## Naming Conventions
- camelCase or matching the library name (e.g. `supabase.ts`)