# src/services
## Purpose
Core logic files for communicating with APIs, databases, or local devices.
## Allowed Files
- API files (e.g. `jobs.ts`, `payments.ts`)
## Naming Conventions
- camelCase or kebab-case (e.g. `job-service.ts`)