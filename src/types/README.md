# src/types
## Purpose
TypeScript type interfaces, type unions, schemas, and structural definitions.
## Allowed Files
- TypeScript declarations and types (.d.ts, .ts)
## Naming Conventions
- kebab-case (e.g. `user.ts`, `database.types.ts`)
- PascalCase for type/interface/enum names inside files