# src/stores
## Purpose
Zustand global stores managing client-side reactive state.
## Allowed Files
- Zustand store definitions (e.g. `authStore.ts`, `themeStore.ts`)
## Naming Conventions
- camelCase ending with "Store" (e.g. `useThemeStore.ts`)