# src/providers
## Purpose
React Context Providers wrapping the application (e.g. Query client provider, Auth context, theme controllers).
## Allowed Files
- Providers (e.g. `QueryProvider.tsx`, `ThemeProvider.tsx`, `AuthProvider.tsx`)
## Naming Conventions
- PascalCase ending with "Provider" (e.g. `QueryProvider.tsx`)