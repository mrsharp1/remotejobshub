# src/routes
## Purpose
Configures client-side routing, route mappings, navigation protection, and route configuration lists.
## Allowed Files
- Routing configs (e.g. `index.tsx`, `paths.ts`)
## Naming Conventions
- camelCase or kebab-case (e.g. `AppRoutes.tsx`)