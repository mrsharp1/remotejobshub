# src/layouts
## Purpose
Visual wrappers and templates for different parts of the application (e.g. Auth flow, Dashboard, Public).
## Allowed Files
- Layout files (e.g. `MainLayout.tsx`, `DashboardLayout.tsx`)
## Naming Conventions
- PascalCase (e.g. `MainLayout.tsx`)