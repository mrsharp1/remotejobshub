# src/components/ui
## Purpose
Houses base, atomic, unstyled or shadcn-styled components.
## Allowed Files
- Standalone components (e.g. `button.tsx`, `input.tsx`, `dialog.tsx`)
## Naming Conventions
- kebab-case for files (e.g. `button.tsx`)
- PascalCase for React component exports (e.g. `Button`)