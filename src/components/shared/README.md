# src/components/shared
## Purpose
Reusable application components that represent custom composite controls (e.g. Card, Modal, Loader) but are not generic atomic UI.
## Naming Conventions
- PascalCase (e.g. `JobCard.tsx`)