# src/components/layout
## Purpose
Contains structural layout pieces like Navbars, Footers, and Sidebars that frame the screen layouts.
## Allowed Files
- Layout-specific components (e.g. `Navbar.tsx`, `Sidebar.tsx`, `Footer.tsx`)
## Naming Conventions
- PascalCase (e.g. `Navbar.tsx`)