# src/hooks
## Purpose
Global reusable React hooks.
## Allowed Files
- Custom hooks (e.g. `useAuth.ts`, `useLocalStorage.ts`)
## Naming Conventions
- camelCase starting with "use" (e.g. `useWindowSize.ts`)