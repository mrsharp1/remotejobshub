# src/features
## Purpose
Feature-based modular modules. Holds components, pages, hooks, and services related to specific business domains.
## Allowed Folders
- Subdirectories per feature (e.g. `jobs/`, `billing/`, `auth/`)
## Naming Conventions
- kebab-case for directories